# Python
__pycache__/
*.pyc
dist/
build/
*.egg-info/
.pytest_cache/

# Node
node_modules/
dist-ui/
*.timestamp-*.mjs

# Rust / Tauri
src-tauri/target/
src-tauri/gen/

# DB / runtime
mkw_tracker.db
*.db-shm
*.db-wal
bot-state.json

# Secrets (the .example templates are committed; the real .env is not)
.env

# Debug outputs
debug_laps/

# Legacy monolith reference (local only)
\!!!FINAL-ab-new-bubbles.py

# Copied legacy server (reference only; contains secrets: service_account_key.json, .env)
legacy/

# Probe / scratch files
probe_*.py
probe_*.png
probe_*.bin
test_stream.bin

# Tauri signing keys (should live in ~/.tauri/, never in the repo)
*.key
*.key.pub

# Misplaced home-dir shorthand (literal ~/  folder)
~/

# Claude Code local state
.claude/

# Tauri sidecar binaries (built by CI)
src-tauri/binaries/
src-tauri/sidecar/

# IDEs
.vscode/
.idea/
*.swp

# Tool .venv
tools/autotemplate/.venv/
old_assets/

# Testing
*.mp4
temp/
.superpowers/

# Asset capture data (large binary; local only — clips/PNGs written by the sweep).
# Glob covers the bright set + paired/backup variants (captures_sdr_dark, _old, _new_old, ...).
captures_sdr*/
